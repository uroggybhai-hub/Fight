# 1. FFmpeg install (MANDATORY)
sudo apt update
sudo apt install ffmpeg python3-pip -y

# 2. Pip install
pip install --upgrade pip
pip install -r requirements.txt

# 3. Session file generate karo (pehli baar)
python3 figkt.py
# Login karne ke liye phone number enter karo

# 4. Background mein run karo
screen -S vc-bot
python3 figkt.py
# Ctrl+A, D se nikal jao
